# SRX Platform - Systemic Risk Exchange
# Install with: pip install -r requirements.txt

# Interactive dashboard
streamlit==1.41.1

# Financial data downloader (Yahoo Finance)
# yfinance uses curl_cffi internally for TLS fingerprinting
# to bypass Yahoo's bot detection on cloud servers.
yfinance==1.2.0
curl_cffi>=0.7.5

# Data manipulation (tables and math)
pandas==2.2.3
numpy==1.26.4

# Interactive charts
plotly==5.24.1

# Date handling utilities
python-dateutil==2.9.0